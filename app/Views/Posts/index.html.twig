{% extends layout %}
{% block content %}
  Blog Posts
  {% for post in posts %}
  <div id="{{ post.id }}">
    <a href="/blog/{{ post.slug }}">{{ post.title }}</a>
  </div>
  {% endfor %}
{% endblock %}